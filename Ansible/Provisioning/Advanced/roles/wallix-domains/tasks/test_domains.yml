---
# Test domain connectivity tasks

- name: "Domains | Test | Check domain status"
  debug:
    msg: "Domain connectivity test skipped in test mode"
  when: wallix_domains.operation_mode | default('apply') == 'validate_only'

- name: "Domains | Test | Set test status"
  set_fact:
    wallix_domains_test_passed: true